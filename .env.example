# Copy to .env and set a real password before running docker compose up.
# The container binds 0.0.0.0, so ip-watch requires auth (or IPWATCH_INSECURE=1).
IPWATCH_AUTH_USERNAME=admin
IPWATCH_AUTH_PASSWORD=
